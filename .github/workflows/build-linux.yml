# SPDX-License-Identifier: MPL-2.0
name: Build Linux
on:
  workflow_call:

env:
  CMAKE_COMMON: >-
    -G Ninja
    -DPRISM_ENABLE_TESTS=OFF
    -DPRISM_ENABLE_DEMOS=OFF
    -DPRISM_ENABLE_LEGACY_BACKENDS=ON
    -DPRISM_ENABLE_SHIMS=ON

jobs:
  linux:
    strategy:
      fail-fast: true
      matrix:
        include:
          - { name: x64,   os: ubuntu-latest }
          - { name: arm64, os: ubuntu-24.04-arm }
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y libspeechd-dev libglib2.0-dev libglibmm-2.68-dev wine64-tools libdbus-1-dev build-essential meson ninja-build pkg-config git libglib2.0-bin libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev libgirepository1.0-dev gobject-introspection gi-docgen dbus-daemon python3 python3-tap python3-dbusmock
      - name: Install current CMake
        uses: lukka/get-cmake@v4.3.3
        with:
          cmakeVersion: latest
      - name: Build all configurations
        run: |
          for shared in ON OFF; do
            linkage=$( [ "$shared" = "ON" ] && echo "dynamic" || echo "static" )
            for bt in Release Debug; do
              config=$(echo "$bt" | tr '[:upper:]' '[:lower:]')
              cmake -B "build-${linkage}-${config}" ${{ env.CMAKE_COMMON }} \
                -DCMAKE_BUILD_TYPE="$bt" \
                -DBUILD_SHARED_LIBS="$shared" \
                -DPRISM_BUILD_WINELIBS=ON \
                -DPRISM_ENABLE_GDEXTENSION=OFF
              cmake --build "build-${linkage}-${config}"
            done
          done
      - name: Package
        run: |
          for linkage in dynamic static; do
            for config in release debug; do
              bt=$( [ "$config" = "release" ] && echo Release || echo Debug )
              rm -rf "dist/${linkage}/${config}"
              cmake --install "build-${linkage}-${config}" \
                    --prefix  "dist/${linkage}/${config}" \
                    --config  "$bt"
            done
          done
          mv dist/dynamic/release/include dist/include
          rm -rf dist/dynamic/debug/include dist/static/release/include dist/static/debug/include
          cp -r LICENSES dist
          cp NOTICE dist
          cd dist && zip -r "../prism-linux-${{ matrix.name }}.zip" .
      - uses: actions/upload-artifact@v7
        with:
          name: linux-${{ matrix.name }}
          path: prism-linux-${{ matrix.name }}.zip
