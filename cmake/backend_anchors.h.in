// SPDX-License-Identifier: MPL-2.0

// Generated by cmake/PrismBackends.cmake, included by source/prism.cpp.
//
// One directive per backend that was actually built, naming the anchor symbol
// that backend's object emits (see PRISM_EMIT_ANCHOR in
// source/backend_catalog.h). MSVC keeps an object only when something
// references it, and a registrar that registers itself is referenced by
// nothing, so a static build would otherwise drop every backend and leave the
// catalog empty.
//
// These live in a header rather than a source file because the linker reads
// directives only from objects it has already decided to link. In an object
// of their own they would be dropped for exactly the reason they exist to
// prevent.

#pragma once

@_anchors@
