if(PRISM_ENABLE_TOLK_SHIM)
  add_subdirectory(tolk)
endif()
if(PRISM_ENABLE_UNIVERSAL_SPEECH_SHIM)
  add_subdirectory(universal_speech)
endif()
if(PRISM_ENABLE_SPEECH_CORE_SHIM)
  add_subdirectory(speech_core)
endif()
if(PRISM_ENABLE_SRAL_SHIM)
  add_subdirectory(sral)
endif()
